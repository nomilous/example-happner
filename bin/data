#!/usr/bin/env node

require('dotenv-safe').load();

var Happner = require('happner');
var config = require('../config/data');
var server = require('../lib/data');

Happner.create(config).then(server).catch(function(error) {
  console.error(error);
  process.exit(1);
});
